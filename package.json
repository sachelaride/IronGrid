{
  "name": "irongrid",
  "version": "12",
  "private": true,
  "license": "GPL-3.0-or-later",
  "description": "Open source network and IT infrastructure monitoring, inventory and management for Linux and Docker",
  "keywords": [
    "network-monitoring",
    "network-management",
    "infrastructure-monitoring",
    "snmp",
    "network-inventory",
    "linux",
    "docker",
    "open-source",
    "it-infrastructure"
  ],
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build -w @irongrid/server && npm run build -w @irongrid/web && npm run build -w irongrid-agent",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "turbo": "^1.11.2"
  }
}
